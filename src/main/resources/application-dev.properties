# ===============================================================
# DEVELOPMENT SERVER CONFIGURATION
# ===============================================================
# This file overrides settings from application.properties when the 'dev' profile is active.

server.port=8081

# Connect to the development database
spring.datasource.url=jdbc:mariadb://localhost:3306/technik_team_db_dev?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=technik_user_dev
spring.datasource.password=X32MYaaD
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# App Base URL (for generating absolute URLs in emails, iCal, etc.) - DEV
app.base-url=https://technikteamdev.qs0.de

# WebAuthn Relying Party Configuration - DEV
webauthn.relying-party.id=technikteamdev.qs0.de
webauthn.relying-party.name=TechnikTeam (Dev)
webauthn.relying-party.origins=https://technikteamdev.qs0.de