# Database Connection Details
# The hostname is 'localhost' because we are connecting from the Eclipse Tomcat on the host machine.
# The user is 'technik_user' to match the docker-compose.yml file.
db.url=jdbc:mariadb://localhost:3306/technik_team_db?useUnicode=true&characterEncoding=UTF-8&useSSL=false
db.user=technik_user
db.password=X32MYaaD

# File Upload Configuration
# This path is now a local path on your Windows machine, as Tomcat runs locally.
upload.directory=C:/dev/eclipse/workspace/resources/uploads
upload.temp.directory=C:/dev/eclipse/workspace/resources/uploads/temp

# Collabora Integration
collabora.server.url=http://localhost:9980