# Server Configuration
server.port=8080

# Database Configuration (using HikariCP)
spring.datasource.url=jdbc:mariadb://localhost:3306/technik_team_db
spring.datasource.username=technik_user
spring.datasource.password=X32MYaaD
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Flyway Database Migration
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# springdoc-openapi (Swagger UI) Configuration
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# Custom Application Properties (can be accessed via @Value or ConfigurationProperties)
jwt.secret=your-super-secret-key-for-jwt-that-is-long-and-secure
upload.directory=C:/technikteam_uploads