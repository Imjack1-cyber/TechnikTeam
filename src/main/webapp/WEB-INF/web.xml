<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <!-- Konfigurationen für Session, Welcome-File und JSP bleiben unverändert -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>
    
    <welcome-file-list>
        <welcome-file>login.jsp</welcome-file>
    </welcome-file-list>
    
    <jsp-config>
        <jsp-property-group>
            <url-pattern>*.jsp</url-pattern>
            <url-pattern>*.jspf</url-pattern>
            <el-ignored>false</el-ignored>
        </jsp-property-group>
    </jsp-config>

    <!-- =================================================================== -->
    <!--          FINALE, GARANTIERTE FILTER-KONFIGURATION                 -->
    <!-- =================================================================== -->
    <!-- Die Reihenfolge der <filter-mapping>-Blöcke legt die Ausführungsreihenfolge fest. -->

    <!-- 1. CharacterEncodingFilter: Wird IMMER als ALLERERSTES ausgeführt. -->
    <filter>
        <filter-name>CharacterEncodingFilter</filter-name>
        <filter-class>de.technikteam.filter.CharacterEncodingFilter</filter-class>
        <async-supported>true</async-supported>
    </filter>
    <filter-mapping>
        <filter-name>CharacterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- 2. AuthenticationFilter: Wird als ZWEITES ausgeführt. -->
    <filter>
        <filter-name>AuthenticationFilter</filter-name>
        <filter-class>de.technikteam.filter.AuthenticationFilter</filter-class>
        <async-supported>true</async-supported>
    </filter>
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- 3. AdminFilter: Wird als DRITTES ausgeführt, aber nur für /admin/* Pfade. -->
    <filter>
        <filter-name>AdminFilter</filter-name>
        <filter-class>de.technikteam.filter.AdminFilter</filter-class>
        <async-supported>true</async-supported>
    </filter>
    <filter-mapping>
        <filter-name>AdminFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>


    <!-- Fehlerseiten -->
    <error-page>
        <error-code>404</error-code>
        <location>/error404.jsp</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/error500.jsp</location>
    </error-page>

</web-app>